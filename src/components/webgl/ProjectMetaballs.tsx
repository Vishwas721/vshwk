"use client";

/**
 * ProjectMetaballs
 *
 * Retired in favor of the pure GSAP DOM-based "Bubble Blast" circular wipe
 * (ProjectsPickupSection.tsx) as requested in design pivot.
 * Kept as a lightweight placeholder for future 3D canvas additions.
 */
export default function ProjectMetaballs() {
  return null;
}
